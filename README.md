# ipo-desk — data branch

Collector output only. `latest.json` is the validated document; `history/` holds daily restore points
(45 days, Sundays kept). Every commit here is made by `ipo-desk-bot` from `.github/workflows/collect.yml`
after `scripts/validate.py` passed. Code, research sheets and everything humans edit live on `main`.
